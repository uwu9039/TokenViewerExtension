# 构建产物与本地状态（发布源码前必须排除，内含本机绝对路径）
bin/
obj/
.vs/
*.user

# 本地数据（含 API Key / Token，切勿提交）
*.json
!Directory.Packages.props
!nuget.config
!launchSettings.json

# 构建脚本生成的临时安装脚本
TokenViewerExtension/setup-x64.iss
TokenViewerExtension/setup-arm64.iss

# 本地 MSIX 打包产物
TokenViewerExtension/AppPackages/
